package covidtracker.covidtracker.model;

public class Tests extends Stats {

    // When there's no info about some statistic, it's value is -1
    
    public Tests(double oneMPop, int total) {
        super(oneMPop, total);
    }
}
